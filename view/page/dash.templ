package page

import (
  "template-inator/view/layout"
	"template-inator/model"
	"template-inator/view/component"
)

templ Dash(user model.User) {
    @layout.Base() {
<div class="flex pb-6 mt-8 md:mt-0">
  <h1>{ user.Email}</h1>
</div>
<div class="flex flex-row flex-wrap gap-8">
</div>
        @component.Input("text", "Hellp")
    }
}
